export default function Tag({ children }) {
  return (
    <span className="inline-block rounded-full bg-white/6 px-2.5 py-1 text-[10.5px] font-medium text-text-secondary">
      {children}
    </span>
  );
}
